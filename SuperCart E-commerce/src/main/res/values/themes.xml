<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SuperCart" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
    </style>

    <style name="Theme.SuperCart" parent="Base.Theme.SuperCart" />

    <style name="Theme.AppCompat.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_splash_screen</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_splash_screen</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.SuperCart</item>

    </style>
</resources>